image: node:latest

pages:
  stage: deploy
  script:
  - npm cache clean --force
  - npm install -g npm
  - npm run build
  - rm -rf public
  - mv build public
  artifacts:
    paths:
    - public
  only:
  - master