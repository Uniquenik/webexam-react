image: node:latest

pages:
  stage: deploy
  script:
  - npm install
  - npm run build
  - rm -rf public
  - mv build public
  variables: 
    CI: "true"
  artifacts:
    paths:
    - public
  only:
  - master